import BlogPostForm from '@/components/features/admin/BlogPostForm';

export const metadata = {
    title: 'Tulis Artikel Baru - Admin Panel',
};

export default function CreateBlogPostPage() {
    return (
        <BlogPostForm />
    );
}
